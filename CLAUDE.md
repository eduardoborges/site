# site

Site pessoal (eduardoborges.dev). Astro estático, deploy no Cloudflare Pages.

## Deploy

Push na `main` já dispara o deploy via GitHub Actions (`.github/workflows/cd.yml`).
Não precisa rodar `npm run ship` — ele existe só como fallback manual.
