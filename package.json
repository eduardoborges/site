{
  "name": "site",
  "type": "module",
  "version": "2.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "ship": "astro build && wrangler pages deploy dist --project-name=site --branch=main"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.0",
    "@fontsource-variable/jetbrains-mono": "^5.3.0",
    "astro": "^5.18.0",
    "rehype-external-links": "^3.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.118.0"
  }
}